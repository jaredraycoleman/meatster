{
  "name": "kaani",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "generate-data": "npx tsx scripts/generate-data.ts",
    "check-updates": "npx tsx scripts/check-for-updates.ts",
    "build": "npm run generate-data && tsc -b && vite build",
    "build:skip-data": "tsc -b && vite build",
    "lint": "eslint .",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:skip-data": "npm run build:skip-data && wrangler pages deploy dist",
    "preview": "npm run build && wrangler pages dev dist",
    "preview:skip-data": "npm run build:skip-data && wrangler pages dev dist"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.62.0",
    "date-fns": "^4.1.0",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "eslint": "^9.17.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.16",
    "globals": "^15.14.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.0.0",
    "tsx": "^4.7.0",
    "typescript": "~5.6.2",
    "typescript-eslint": "^8.18.2",
    "vite": "^6.0.5",
    "wrangler": "^3.99.0"
  }
}
